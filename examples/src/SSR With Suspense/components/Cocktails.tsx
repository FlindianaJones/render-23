import styles from '../styles/Cocktails.module.css'
// Provide way to switch how you search
// Provide input for what to search on
// Search, show results all pretty like

const Cocktails = () => {
  return (
    <div className={styles.container}>
      <span>Constructin</span>
    </div>
  )
}

export default Cocktails